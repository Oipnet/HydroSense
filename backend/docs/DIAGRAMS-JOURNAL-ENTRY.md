# 📐 Diagrammes et Architecture - JournalEntry

Ce document contient des schémas visuels pour mieux comprendre l'architecture de JournalEntry.

---

## 🗄️ Modèle de données

```
┌─────────────────┐
│      User       │
│─────────────────│
│ id              │
│ email           │
│ name            │
└────────┬────────┘
         │ 1
         │ owns
         │
         │ N
┌────────▼────────┐
│      Farm       │
│─────────────────│
│ id              │
│ name            │
│ owner_id        │
└────────┬────────┘
         │ 1
         │ has
         │
         │ N
┌────────▼────────┐
│   Reservoir     │◄────────────────┐
│─────────────────│                 │
│ id              │                 │
│ name            │                 │
│ farm_id         │                 │
│ volumeLiters    │                 │
└────────┬────────┘                 │
         │ 1                        │
         │ has                      │
         │                          │
         │ N                        │ N
┌────────▼────────┐        ┌────────┴────────┐
│  Measurement    │        │  JournalEntry   │ ⭐ NOUVEAU
│─────────────────│        │─────────────────│
│ id              │        │ id              │
│ reservoir_id    │        │ reservoir_id    │
│ ph              │        │ content         │
│ ec              │        │ photoUrl        │
│ waterTemp       │        │ createdAt       │
│ measuredAt      │        │ updatedAt       │
└─────────────────┘        └─────────────────┘
```

---

## 🔄 Flux de création d'une entrée de journal

```
┌──────────────┐
│   Client     │
│  (Frontend)  │
└──────┬───────┘
       │
       │ POST /api/journal_entries
       │ {
       │   "reservoir": "/api/reservoirs/1",
       │   "content": "...",
       │   "photoUrl": "..."
       │ }
       │
       ▼
┌──────────────────────────────────────────────┐
│           API Platform                       │
│──────────────────────────────────────────────│
│  1. Vérification JWT (Firewall)             │
│     ✓ Token valide ?                         │
│     ✓ User authentifié ?                     │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  2. Dénormalisation (Deserializer)          │
│     - Convertir JSON en objet JournalEntry  │
│     - Résoudre IRI (/api/reservoirs/1)      │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  3. Validation (Symfony Validator)          │
│     ✓ content NotBlank ?                     │
│     ✓ content <= 5000 caractères ?          │
│     ✓ reservoir NotNull ?                    │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  4. Security Post-Denormalize                │
│     ✓ User possède le reservoir ?           │
│     ✓ Reservoir → Farm → Owner == User ?    │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  5. Lifecycle Callbacks                      │
│     - createdAt = new DateTimeImmutable()   │
│     - updatedAt = new DateTimeImmutable()   │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  6. Persistence (Doctrine)                   │
│     - INSERT INTO journal_entry ...          │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  7. Normalisation (Serializer)              │
│     - Convertir objet en JSON                │
│     - Appliquer groupes de sérialisation     │
└──────┬───────────────────────────────────────┘
       │
       │ 201 Created
       │ {
       │   "@id": "/api/journal_entries/1",
       │   "id": 1,
       │   "content": "...",
       │   "createdAt": "2025-11-20T...",
       │   ...
       │ }
       ▼
┌──────────────┐
│   Client     │
└──────────────┘
```

---

## 🔒 Flux de sécurité (GET collection)

```
┌──────────────┐
│   Client     │
└──────┬───────┘
       │
       │ GET /api/journal_entries
       │
       ▼
┌──────────────────────────────────────────────┐
│  1. JWT Verification                         │
│     ✓ Token valide ?                         │
│     ✓ ROLE_USER ?                            │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  2. Query Builder Initial                    │
│     SELECT * FROM journal_entry              │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  3. JournalEntryQueryExtension               │
│──────────────────────────────────────────────│
│  INNER JOIN reservoir                        │
│      ON journal_entry.reservoir_id = ...     │
│  INNER JOIN farm                             │
│      ON reservoir.farm_id = ...              │
│  WHERE farm.owner_id = :current_user         │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  4. Exécution SQL                            │
│     - Récupère uniquement les entrées        │
│       des réservoirs de l'utilisateur        │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  5. Normalisation & Réponse                  │
│     - Convertir en JSON                      │
│     - Appliquer groupes "journal:read"       │
└──────┬───────────────────────────────────────┘
       │
       │ 200 OK
       │ [
       │   { "@id": "/api/journal_entries/1", ... },
       │   { "@id": "/api/journal_entries/2", ... }
       │ ]
       ▼
┌──────────────┐
│   Client     │
└──────────────┘
```

---

## 🛡️ Niveaux de sécurité

```
┌─────────────────────────────────────────────────────┐
│  Niveau 1 : API Platform Security Expression       │
│─────────────────────────────────────────────────────│
│  Appliqué AVANT l'accès à l'objet                  │
│  ✓ Vérifie ROLE_USER                               │
│  ✓ Vérifie ownership après chargement              │
│                                                     │
│  security: "is_granted('ROLE_USER') and             │
│             object.getReservoir()                   │
│                   .getFarm()                        │
│                   .getOwner() == user"              │
└─────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────┐
│  Niveau 2 : Security Post-Denormalize              │
│─────────────────────────────────────────────────────│
│  Appliqué APRÈS dénormalisation (POST)             │
│  ✓ Vérifie que l'objet créé appartient au user    │
│                                                     │
│  securityPostDenormalize:                           │
│      "is_granted('ROLE_USER') and                   │
│       object.getReservoir()                         │
│             .getFarm()                              │
│             .getOwner() == user"                    │
└─────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────┐
│  Niveau 3 : Query Extension                         │
│─────────────────────────────────────────────────────│
│  Appliqué AUTOMATIQUEMENT sur toutes les requêtes  │
│  ✓ Filtre les résultats en base de données         │
│  ✓ Impossible de voir les données des autres       │
│                                                     │
│  JournalEntryQueryExtension:                        │
│      WHERE farm.owner = :current_user               │
└─────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────┐
│         SÉCURITÉ GARANTIE ✅                        │
│─────────────────────────────────────────────────────│
│  • User A ne peut voir que ses entrées             │
│  • User B ne peut pas créer d'entrée pour User A  │
│  • User C ne peut pas modifier d'entrée de User A │
│  • Admins peuvent tout voir (bypass niveau 3)     │
└─────────────────────────────────────────────────────┘
```

---

## 📊 Groupes de sérialisation

```
┌─────────────────────────────────────────────┐
│           JournalEntry Entity               │
├─────────────────────────────────────────────┤
│  id                  [journal:read]         │
│  reservoir           [journal:read, write]  │
│  content             [journal:read, write]  │
│  photoUrl            [journal:read, write]  │
│  createdAt           [journal:read]         │
│  updatedAt           [journal:read]         │
└─────────────────────────────────────────────┘
            │                   │
            │                   │
    ┌───────▼─────┐     ┌──────▼──────┐
    │  GET        │     │  POST/PUT   │
    │  (Lecture)  │     │  (Écriture) │
    ├─────────────┤     ├─────────────┤
    │ journal:    │     │ journal:    │
    │   read      │     │   write     │
    └─────────────┘     └─────────────┘
            │                   │
            ▼                   ▼
    ┌─────────────┐     ┌─────────────┐
    │ Retourne :  │     │ Accepte :   │
    │ • id        │     │ • reservoir │
    │ • reservoir │     │ • content   │
    │ • content   │     │ • photoUrl  │
    │ • photoUrl  │     └─────────────┘
    │ • createdAt │
    │ • updatedAt │
    └─────────────┘
```

---

## 🔄 Cycle de vie d'une entité

```
┌─────────────────────────────────────────────────┐
│               CRÉATION (POST)                   │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ __construct()  │
        │ ────────────── │
        │ createdAt = NOW│
        │ updatedAt = NOW│
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │ Validation     │
        │ ────────────── │
        │ ✓ content      │
        │ ✓ reservoir    │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │ @ORM\PrePersist│
        │ (automatique)  │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │  INSERT INTO   │
        │  journal_entry │
        └────────────────┘

┌─────────────────────────────────────────────────┐
│           MODIFICATION (PUT/PATCH)              │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ setContent()   │
        │ setPhotoUrl()  │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │ Validation     │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │ @ORM\PreUpdate │
        │ ────────────── │
        │ updatedAt = NOW│ ⭐ AUTOMATIQUE
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │  UPDATE        │
        │  journal_entry │
        └────────────────┘

┌─────────────────────────────────────────────────┐
│              SUPPRESSION (DELETE)               │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ Security check │
        │ ✓ owner?       │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │  DELETE FROM   │
        │  journal_entry │
        └────────────────┘
```

---

## 🏗️ Architecture des classes

```
┌────────────────────────────────────────────────┐
│          JournalEntry (Entity)                 │
│────────────────────────────────────────────────│
│ - id: ?int                                     │
│ - reservoir: ?Reservoir                        │
│ - content: ?string                             │
│ - photoUrl: ?string                            │
│ - createdAt: ?DateTimeImmutable                │
│ - updatedAt: ?DateTimeImmutable                │
│────────────────────────────────────────────────│
│ + __construct()                                │
│ + getId(): ?int                                │
│ + getReservoir(): ?Reservoir                   │
│ + setReservoir(?Reservoir): static             │
│ + getContent(): ?string                        │
│ + setContent(string): static                   │
│ + getPhotoUrl(): ?string                       │
│ + setPhotoUrl(?string): static                 │
│ + getCreatedAt(): ?DateTimeImmutable           │
│ + setCreatedAt(DateTimeImmutable): static      │
│ + getUpdatedAt(): ?DateTimeImmutable           │
│ + setUpdatedAt(DateTimeImmutable): static      │
│ + setUpdatedAtValue(): void [PreUpdate]        │
└────────────────────────────────────────────────┘
                        │
                        │ ManyToOne
                        ▼
┌────────────────────────────────────────────────┐
│            Reservoir (Entity)                  │
│────────────────────────────────────────────────│
│ - journalEntries: Collection<JournalEntry>     │
│────────────────────────────────────────────────│
│ + getJournalEntries(): Collection              │
│ + addJournalEntry(JournalEntry): static        │
│ + removeJournalEntry(JournalEntry): static     │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│    JournalEntryRepository (Repository)         │
│────────────────────────────────────────────────│
│ + findByUser(int): JournalEntry[]              │
│ + findByReservoir(int): JournalEntry[]         │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│  JournalEntryQueryExtension (Extension)        │
│────────────────────────────────────────────────│
│ Implements:                                    │
│   - QueryCollectionExtensionInterface          │
│   - QueryItemExtensionInterface                │
│────────────────────────────────────────────────│
│ + applyToCollection(...)                       │
│ + applyToItem(...)                             │
│ - addWhere(...)                                │
└────────────────────────────────────────────────┘
```

---

## 📁 Structure des fichiers

```
backend/
│
├── src/
│   ├── Entity/
│   │   ├── JournalEntry.php ⭐ NOUVEAU
│   │   │   ├── Annotations API Platform
│   │   │   ├── Annotations Doctrine
│   │   │   ├── Annotations Validator
│   │   │   └── Lifecycle callbacks
│   │   │
│   │   └── Reservoir.php ✏️ MODIFIÉ
│   │       └── + journalEntries: Collection
│   │
│   ├── Repository/
│   │   └── JournalEntryRepository.php ⭐ NOUVEAU
│   │       ├── findByUser(userId)
│   │       └── findByReservoir(reservoirId)
│   │
│   └── Extension/
│       └── JournalEntryQueryExtension.php ⭐ NOUVEAU
│           ├── applyToCollection()
│           ├── applyToItem()
│           └── addWhere() [private]
│
├── migrations/
│   └── Version20251120115107.php ⭐ NOUVEAU
│       ├── up() : CREATE TABLE
│       └── down() : DROP TABLE
│
├── docs/
│   ├── README-JOURNAL-ENTRY.md ⭐ NOUVEAU
│   ├── EPIC-2-JOURNAL-ENTRY-IMPLEMENTATION.md ⭐ NOUVEAU
│   ├── TESTING-JOURNAL-ENTRY-API.md ⭐ NOUVEAU
│   ├── QUICKSTART-JOURNAL-ENTRY.md ⭐ NOUVEAU
│   └── DIAGRAMS-JOURNAL-ENTRY.md ⭐ CE FICHIER
│
├── examples/
│   └── journal_entries_examples.md ⭐ NOUVEAU
│
├── SYNTHESE-JOURNAL-ENTRY.md ⭐ NOUVEAU
├── INDEX-JOURNAL-ENTRY.md ⭐ NOUVEAU
└── README.md ✏️ MODIFIÉ
```

---

## 🎯 Cas d'utilisation

```
┌─────────────────────────────────────────────────┐
│           CAS 1 : Création normale              │
└─────────────────────────────────────────────────┘

User A (id=1)
  │
  │ possède
  ▼
Farm (id=1, owner_id=1)
  │
  │ contient
  ▼
Reservoir (id=1, farm_id=1)
  │
  │ User A crée une entrée
  ▼
JournalEntry (id=1, reservoir_id=1)
  content: "pH ajusté à 6.5"
  createdAt: 2025-11-20 10:00:00

✅ SUCCESS


┌─────────────────────────────────────────────────┐
│      CAS 2 : Tentative d'accès non autorisé    │
└─────────────────────────────────────────────────┘

User A (id=1)               User B (id=2)
  │                           │
  │ possède                   │ possède
  ▼                           ▼
Farm (id=1)                 Farm (id=2)
  │                           │
  │ contient                  │
  ▼                           │
Reservoir (id=1)              │
  │                           │
  │ contient                  │
  ▼                           │
JournalEntry (id=1)           │
                              │
                              │ User B tente de lire
                              │ JournalEntry #1
                              ▼
                        ┌──────────────┐
                        │ QueryExtension│
                        │──────────────│
                        │ WHERE        │
                        │ farm.owner   │
                        │    = User B  │
                        └──────────────┘
                              │
                              ▼
                        Aucun résultat

❌ DENIED (404 Not Found)


┌─────────────────────────────────────────────────┐
│     CAS 3 : Tentative de création non autorisée│
└─────────────────────────────────────────────────┘

User B (id=2)
  │
  │ tente de créer
  ▼
POST /api/journal_entries
{
  "reservoir": "/api/reservoirs/1",  ← appartient à User A
  "content": "..."
}
  │
  │ après dénormalisation
  ▼
JournalEntry (temporaire, non persisté)
  reservoir: Reservoir #1 (owner: User A)
  │
  │ securityPostDenormalize check
  ▼
object.getReservoir()           // Reservoir #1
      .getFarm()                // Farm #1
      .getOwner() == user       // User A != User B

❌ DENIED (403 Forbidden)
```

---

## 📈 Timeline d'une requête (diagramme séquence)

```
Client   API Platform   Validator   Security   QueryExtension   Doctrine   Database
  │            │             │          │             │            │          │
  │ GET /api/journal_entries │          │             │            │          │
  ├───────────>│             │          │             │            │          │
  │            │ Check JWT   │          │             │            │          │
  │            ├─────────────────────>  │             │            │          │
  │            │<────────────────────── │             │            │          │
  │            │        ✅ ROLE_USER    │             │            │          │
  │            │             │          │             │            │          │
  │            │ Build Query │          │             │            │          │
  │            ├─────────────────────────────────────>│            │          │
  │            │             │          │             │ Add WHERE  │          │
  │            │             │          │             │ owner=user │          │
  │            │<─────────────────────────────────────┤            │          │
  │            │             │          │             │            │          │
  │            │ Execute Query                        │            │          │
  │            ├───────────────────────────────────────────────────>          │
  │            │             │          │             │            │          │
  │            │             │          │             │            │ SELECT * │
  │            │             │          │             │            │ FROM ... │
  │            │             │          │             │            │ WHERE .. │
  │            │<───────────────────────────────────────────────────────────  │
  │            │             │          │             │            │          │
  │            │ Normalize   │          │             │            │          │
  │            │ (to JSON)   │          │             │            │          │
  │            │             │          │             │            │          │
  │ 200 OK     │             │          │             │            │          │
  │<───────────┤             │          │             │            │          │
  │ [...]      │             │          │             │            │          │
```

---

**Légende** :

-   ⭐ : Nouveau fichier/fonctionnalité
-   ✏️ : Fichier modifié
-   ✅ : Validation réussie
-   ❌ : Validation échouée
-   ← : Direction/appartenance
