# API Backend URL
NUXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Application
NODE_ENV=development

# Better Auth Configuration
# Secret pour signer les tokens/sessions (générer une valeur aléatoire en production)
BETTER_AUTH_SECRET=development-secret-change-in-production

# Base URL de l'application Nuxt (utilisée pour les redirects OAuth)
BETTER_AUTH_URL=http://localhost:3000

# Keycloak OIDC Configuration
# Issuer URL du realm Keycloak (sans .well-known)
KEYCLOAK_ISSUER=http://127.0.0.1:8080/realms/hydrosense

# Client ID configuré dans Keycloak (public, PKCE enabled)
KEYCLOAK_CLIENT_ID=hydrosense-web-bff

# Client Secret (vide pour un client public avec PKCE)
KEYCLOAK_CLIENT_SECRET=
